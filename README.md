# Obsidian Earth View

Embedded maps of Earth in Obsidian.

## How to install

- Clone this repo into an Obsidan vault's `.obsidian\plugins` directory.
- `npm i` or to install dependencies
- `npm run dev` to start compilation in watch mode.
